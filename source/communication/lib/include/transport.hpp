#ifndef TRANSPORT_HPP
#define TRANSPORT_HPP

#include "buffer.hpp"
#include "ring-buffer.h"

#include <optional>
/*
class IStreamificator
{
public:
    virtual std::optional<PBuffer> unpack(RingBuffer& ring_buffer);
    virtual PBuffer pack(PBuffer& buffer);

    virtual ~IStreamificator() = default;
};
*/
#endif // TRANSPORT_HPP
